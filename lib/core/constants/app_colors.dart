import 'package:flutter/material.dart';

class AppColors {
  // Background
  static const Color background = Color(0xFFF7F7FA);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surface2 = Color(0xFFF0F0F4);

  // Border & Divider
  static const Color border = Color(0xFFE8E8ED);
  static const Color divider = Color(0xFFE8E8ED);

  // Primary (vibrant purple)
  static const Color primary = Color(0xFF6C5CE7);
  static const Color primaryLight = Color(0xFFA29BFE);

  // Text
  static const Color textPrimary = Color(0xFF1A1A2E);
  static const Color textSecondary = Color(0xFF6E6E80);
  static const Color textTertiary = Color(0xFFA1A1B0);
  static const Color textOnPrimary = Color(0xFFFFFFFF);

  // Macro Ring Colors — refined, sophisticated tones
  static const Color calories = Color(0xFF2ECC71);
  static const Color caloriesLight = Color(0xFFE8F8F0);
  static const Color protein = Color(0xFF3B82F6);
  static const Color proteinLight = Color(0xFFEBF2FF);
  static const Color carbs = Color(0xFFFF9500);
  static const Color carbsLight = Color(0xFFFFF4E6);
  static const Color fats = Color(0xFFAF52DE);
  static const Color fatsLight = Color(0xFFF5EDFB);

  // AI Card Accent
  static const Color aiGlow = Color(0xFFFFD60A);

  // Status Colors
  static const Color success = Color(0xFF34C759);
  static const Color warning = Color(0xFFFFCC02);
  static const Color error = Color(0xFFFF3B30);
  static const Color info = Color(0xFF007AFF);

  // Gradient Colors for Progress Bar
  static const List<Color> progressGradient = [
    Color(0xFF6C5CE7),
    Color(0xFFA29BFE),
    Color(0xFFD56BF1),
  ];

  // Shadow (light mode — subtle gray)
  static const Color shadow = Color(0x14000000);

  // Card Background
  static const Color cardBackground = Color(0xFFFFFFFF);

  // Dashboard redesign accents
  static const Color caloriesDeep = Color(0xFF27AE60);
  static const Color surfaceElevated = Color(0xFFFAFAFC);
  static const Color ringTrack = Color(0xFFF0F0F4);

  // Slider
  static const Color sliderTrack = Color(0xFFE5E5EA);

  // Onboarding specific
  static const Color onboardingCard = Color(0xFFFFFFFF);
  static const Color onboardingCardBorder = Color(0xFFE5E5EA);
}
