import 'package:flutter/material.dart';

class AppColors {
  // Background
  static const Color background = Color(0xFFFFFFFF);
  static const Color surface = Color(0xFFF0F0F0);
  static const Color surface2 = Color(0xFFECE8E8);

  // Border & Divider
  static const Color border = Color(0xFFE5E5EA);
  static const Color divider = Color(0xFFE5E5EA);

  // Primary
  static const Color primary = Color(0xFF000000);
  static const Color primaryLight = Color(0xFF333333);

  // Text
  static const Color textPrimary = Color(0xFF1E1A24);
  static const Color textSecondary = Color(0xFF6E6E80);
  static const Color textTertiary = Color(0xFFA1A1B0);
  static const Color textOnPrimary = Color(0xFFFFFFFF);

  // Macro Ring Colors
  static const Color calories = Color(0xFFDE6969);
  static const Color caloriesLight = Color(0xFFFDECEC);
  static const Color protein = Color(0xFF64993A);
  static const Color proteinLight = Color(0xFFE8F5E0);
  static const Color carbs = Color(0xFFDE9A69);
  static const Color carbsLight = Color(0xFFFDF0E8);
  static const Color fats = Color(0xFF6998DE);
  static const Color fatsLight = Color(0xFFE8F0FD);

  // AI Card Accent
  static const Color aiGlow = Color(0xFFFFFFFF);

  // Status Colors
  static const Color success = Color(0xFF64993A);
  static const Color warning = Color(0xFF000000);
  static const Color error = Color(0xFFDE6969);
  static const Color info = Color(0xFF6998DE);

  // Gradient Colors for Progress Bar
  static const List<Color> progressGradient = [
    Color(0xFF000000),
    Color(0xFF333333),
    Color(0xFF666666),
  ];

  // Shadow (light mode — subtle gray)
  static const Color shadow = Color(0x14000000);

  // Card Background
  static const Color cardBackground = Color(0xFFFFFFFF);

  // Dashboard redesign accents
  static const Color caloriesDeep = Color(0xFFC65F60);
  static const Color surfaceElevated = Color(0xFFFAFAFA);
  static const Color ringTrack = Color(0xFFF0F0F0);

  // Slider
  static const Color sliderTrack = Color(0xFFE5E5EA);

  // Onboarding specific
  static const Color onboardingCard = Color(0xFFFFFFFF);
  static const Color onboardingCardBorder = Color(0xFFE5E5EA);
}
